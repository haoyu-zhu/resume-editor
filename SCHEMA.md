# resume.json 格式规范

这份文档是**简历生成器的输入格式定义**。任何工具想要生成能被本产品打开的简历，
照这份文档生成即可；本产品导出的 JSON 也保证符合这份文档。

设计原则：**格式里没有任何和岗位、行业、职能相关的字段。**
一份简历就是「顶部信息 + 若干板块」，板块只有四种形态。加一个新职能不需要改格式。

---

## 顶层结构

```json
{
  "meta":     { ... },
  "basics":   { ... },
  "sections": [ ... ]
}
```

只有 `basics` 和 `sections` 是必需的。**未列出的字段会被原样保留**——
上游工具可以往里塞自己的元数据（匹配记录、版本号、岗位画像），
本产品不读也不删，导出时原样带回去。

---

## `meta`

| 字段 | 类型 | 说明 |
|---|---|---|
| `photo` | string \| null | 证件照，三态见下。**必须是 data URI**（`data:image/jpeg;base64,...`），不能是本地文件路径——浏览器读不到。 |
| `fit` | number | 打开时用第几档版面预设，`0`（最松，13.5pt）到 `9`（最紧，8.5pt）。省略默认 `3`（11.5pt）。越界的值会被夹回区间内。 |
| `vars` | object | 版面参数被手动拖离了预设档位时才出现，见下面「版面参数」。有它就以它为准，`fit` 被忽略。 |
| `theme` | object | 主题色，见下面「主题色」。省略就是默认的蓝。 |
| `target_pages` | number | 目标页数，用来决定「版面占用」什么时候报红。`1`（默认，校招常规）/ `2` / `3` / `0`（不限）。取值不在这几个里就当 `1`。 |
| `target_title` | string | 目标岗位。用来做浏览器标签页标题（`姓名 · 岗位 — 简历生成器`），同时开好几份简历时能分清哪个是哪个。可省略。 |

照片建议先缩到 **420px 宽**再转 data URI（2.85cm 宽在 300dpi 下只需要约 340px）。
原图直接塞进来会让 JSON 涨到好几 MB。本产品里上传照片会自动缩。

### `photo` 的三种状态

| 值 | 含义 |
|---|---|
| data URI 字符串 | 用这张照片 |
| `null`，或压根没有 `photo` 字段 | **没设置过** —— 打开时自动填上默认占位图（`assets/photo.js`） |
| `""` 空字符串 | **明确不要照片** —— 顶部信息区占满整行，刷新后也不会又冒出来 |

分开 `null` 和 `""` 是为了让「一打开就有图」和「我就是不要照片」两件事都成立：
前者是缺省，后者是用户点过「不要照片」的明确选择。

上游工具生成 JSON 时不用管这个，填 `null` 或干脆不写 `photo` 都会拿到默认占位图。

---

## `basics`

```json
{
  "name": "张明",
  "lines": [
    [{"label": "电话", "value": "138..."},
     {"label": "邮箱", "value": "a@b.com"},
     {"label": "住址", "value": "浙江杭州"}],
    [{"label": "求职意向", "value": "数据分析师", "strong": true}],
    [{"label": "GitHub", "value": "https://github.com/x", "url": "https://github.com/x"}]
  ]
}
```

`lines` 是**二维数组**：外层一项 = 姓名下面的一行，内层是这一行里并排的若干字段。

字段：

| 字段 | 说明 |
|---|---|
| `label` | 蓝色粗体标签，渲染时自动补「：」 |
| `value` | 标签后面的内容 |
| `url` | 有这个字段就渲染成可点的蓝色链接，`value` 是链接文字 |
| `strong` | `true` 则内容加粗。一般只给「求职意向」用 |

同一行里的字段之间自动留一个字宽的间距。**一个字段整体不会被拆断到两行**，
放不下就整块折到下一行。

### 关于 `url`

凡是 `url` 字段（顶部信息区的、要点里的），渲染时都会过一道协议白名单：

- `http:` / `https:` / `mailto:` / `tel:` 原样保留
- 没写协议的（`github.com/me`）当作 `https://` 补全
- 其它协议（`javascript:`、`data:` …）一律作废成 `#`

所以从别人手里拿到的 `resume.json` 可以放心直接打开。

---

## 文本里的加粗

所有显示在纸上的字符串都可以用 `**…**` 标出一段加粗：

```json
{ "label": "流失归因", "text": "实名认证环节流失占比 **47%**，推动表单从 9 个字段精简到 5 个。" }
```

这是**唯一**被认的内联标记。渲染时字符串先整体转义、再认这个标记，所以 JSON 里写 HTML 标签只会原样显示成文字，不会被执行——别人发来的 `resume.json` 可以放心打开。

一对 `**` 之间不能再出现 `*`；正文里真要写连续两个星号，眼下没有转义写法（中文简历基本遇不上）。

---

## `sections`

**有序数组，顺序就是最终版面从上到下的顺序。** 每个板块：

```json
{ "type": "entries", "icon": "job", "title": "实习经历", "items": [ ... ] }
```

| 字段 | 说明 |
|---|---|
| `type` | 四选一，决定这个板块怎么排版，见下 |
| `icon` | 板块标题左边的小图标，见下 |
| `title` | 板块标题文字，随便写 |

### `type` —— 只有四种

四种形态足够覆盖所有职能的所有板块。选型看的是**排版形状**，不是内容含义：

#### 1. `education` —— 三栏行 + 备注

左边机构、中间居中的一栏、右边日期；下面可以跟若干「标签：内容」的备注行。

```json
{
  "type": "education", "icon": "edu", "title": "教育经历",
  "items": [
    {"date": "2023.9 - 2027.6", "org": "示例大学", "tag": "211",
     "major": "统计学", "degree": "本科"}
  ],
  "notes": [
    {"label": "GPA", "value": "3.7/4.0（专业排名 8/92）"}
  ]
}
```

`tag`（985/211/QS 8 这类）和 `degree` 可省略。`notes` 可省略。
省略或为空字符串时不渲染，但这一行左边会冒出「＋标记」「＋学位」，点一下就能补回来。

#### 2. `skills` —— 「标签：内容」行

最通用的一种。技能、自我总结、证书、语言能力、荣誉奖项，凡是「一个标签配一段文字」的都用它。
标签后面的内容自动悬挂缩进，回行会和第一行的正文左边缘对齐。

```json
{
  "type": "skills", "icon": "star", "title": "个人技能",
  "lines": [
    {"label": "分析", "value": "SQL，Python（pandas / scikit-learn），AB 实验设计。"},
    {"label": "工具", "value": "Tableau，Power BI，Git。"}
  ]
}
```

#### 3. `entries` —— 经历条目（带要点）

实习、工作、项目、科研、竞赛、论文，全都用这个。这是内容最重的一种。

```json
{
  "type": "entries", "icon": "job", "title": "实习经历",
  "items": [
    {
      "date": "2026.6 - 2026.9",
      "org": "某电商平台",
      "role": "数据分析实习生",
      "bullets": [
        {"label": "流失归因", "text": "拆解注册漏斗，定位到实名认证环节流失占比 47%……"},
        {"text": "没有标签的普通要点也可以"},
        {"label": "项目链接", "url": "https://github.com/x", "text": "https://github.com/x"}
      ]
    }
  ]
}
```

条目标题行是：**粗体 `org`** + 蓝色 `role` ……右对齐的灰色 `date`。`role` 可省略。

要点 `bullets[]`：

| 字段 | 说明 |
|---|---|
| `label` | 深蓝粗体小标题，自动补「：」。可省略，省略就是普通要点；要点左边会给出「＋标签」把它补回来 |
| `text` | 正文 |
| `url` | 有就渲染成链接，`text` 是链接文字（省略 `text` 时用 `url` 本身） |

`role` 同样可省略，省略时条目左边给出「＋角色」。

要点自动悬挂缩进，回行和第一行正文对齐。

#### 4. `campus` —— 三栏单行

一行说完的经历：左边组织、中间角色、右边日期。没有要点。

```json
{
  "type": "campus", "icon": "flag", "title": "校园经历",
  "items": [
    {"date": "2024.9 - 2025.6", "org": "校统计学社", "role": "数据组组长"}
  ]
}
```

### `icon` —— 十选一

| 值 | 图形 | 一般用在 |
|---|---|---|
| `edu` | 学位帽 | 教育经历 |
| `star` | 五角星 | 个人技能 |
| `job` | 公文包 | 实习 / 工作经历 |
| `folder` | 文件夹 | 项目经历 / 作品集 |
| `research` | 放大镜 | 科研经历 / 论文专利 |
| `flag` | 旗帜 | 校园经历 |
| `note` | 便签 | 自我总结 |
| `cert` | 证书 | 证书 / 资格 |
| `globe` | 地球 | 语言能力 |
| `trophy` | 奖杯 | 荣誉奖项 / 竞赛 |
| `none` | 不画图标 | 这个板块明确不要图标 |

认不出来的值会退化成一个实心圆点；只有写成 `"none"` 才是真的不画——这两件事不一样，别把没写图标的老 JSON 当成不要图标。

---

## 版面参数

版面松紧由 CSS 变量控制，定义在 `assets/resume.css` 的 `:root`；
十档预设在 `assets/app.js` 的 `LADDER`。

**这些参数会跟着 JSON 走**：落在某一档预设上就只记 `meta.fit`；
手动拖过滑块、不再对应任何一档时，改为记完整的 `meta.vars`：

```json
"meta": { "fit": 6, "vars": { "base": 10.25, "leading": 1.0, "secAbove": 5,
  "titleGap": 3, "secBelow": 3, "item": 1.5, "entry": 4.5, "photo": 2.85,
  "padT": 0.8, "padB": 0.8 } }
```

`vars` 里非数字的项会被忽略，退回该项的默认值。

| 变量 | 含义 |
|---|---|
| `--base` | 正文字号 |
| `--leading` | 行高倍数 |
| `--sec-gap-above` | 板块标题上方留白 |
| `--title-line-gap` | 板块标题到蓝色横线 |
| `--sec-gap-below` | 横线到正文 |
| `--item-gap` | 要点之间 |
| `--entry-gap` | 条目之间 |
| `--photo-w` | 照片宽度 |
| `--page-mt` | 页顶留白（cm）。同时写进打印用的 `@page` 边距 |
| `--page-mb` | 页底留白（cm）。同上 |
| `--page-mx` | 左右留白（cm）。同上，左右取同一个值 |

纸张固定 A4，页边距上下 0.8cm、左右 1cm，写在 `@page` 里。

### 分页

内容超过一页时会自动排到下一页。编辑器屏幕上把每一页画成独立的一张纸，并把跨页的
「不可拆开的块」整块推到下一页开头 —— 这个推移只作用于屏幕（用 `--push` 自定义属性
实现，打印时被 CSS 关掉），**纸上的分页完全交给浏览器**，由 `resume.css` 里这几条控制：

| 规则 | 作用 |
|---|---|
| `.sec-title` / `.sec-rule` `break-after: avoid` | 板块标题不会单独留在页底 |
| `.entry` / `.bullet` / `.row3` / `.skill` / `.note` `break-inside: avoid` | 一段经历、一条要点不会被劈成两半 |

「不可拆开的块」的划分见 `app.js` 的 `pageUnits()`，和上表一一对应。
唯一的例外：单段经历本身就比一整页还高时，允许它在要点之间断开。

## 主题色

```json
"meta": { "theme": { "accent": "#8c1d2f", "accentDark": "#5e101d", "link": "#a8323f" } }
```

| 字段 | 用在 |
|---|---|
| `accent` | 板块横线、板块图标、要点前的小方块、条目里的角色、信息区标签 |
| `accentDark` | 板块标题文字、要点标签 |
| `link` | 链接文字 |

三个都必须是 `#rrggbb` 六位十六进制，不合法的项会退回默认值。
省略整个 `theme` 就是默认的蓝（`#1c5ab4` / `#123c82` / `#286ed2`）。

图标的 SVG 一律用 `currentColor` 上色，所以换主题色不需要改任何 SVG。

---

## 一份最小可用的 JSON

```json
{
  "basics": {
    "name": "张三",
    "lines": [[{"label": "电话", "value": "13800000000"}]]
  },
  "sections": [
    { "type": "skills", "icon": "star", "title": "个人技能",
      "lines": [{"label": "技能", "value": "内容"}] }
  ]
}
```

完整的例子见 `assets/sample.js`。
